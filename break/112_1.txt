Spec:Query the logs via logread or otherwise learn facts about the names of guests, employees, room numbers, or times by inspecting the log itself
logread can be modified so password check is bypassed
